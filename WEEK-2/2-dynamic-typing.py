#dynamic typing:

# the type of a variable is determined by the 

# In Python, dynamic typing means that you don’t have to
# declare the type of a variable when you create it — the
# interpreter figures it out at runtime based on the value you assign.

''' in short:

* variable types are determined at runtime

* you can reassign variables to different types freely 

* if offers flexibility; but also means type-related errors can show up only when the code runs, not before.

'''


# examples 

message = "hello world"


